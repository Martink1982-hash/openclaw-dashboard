[build]
command = "npm run generate-data && npm run build"
publish = ".next"
functions = "netlify/functions"

[functions]
# Keep runtime dependencies bundled for Netlify Functions output
external_node_modules = ["next"]

[[redirects]]
from = "/api/*"
to = "/.next/server/:splat"
status = 200
